# Information

Эта страница отображается из `configs/information.md`.

- Измените файл, чтобы обновить содержимое без пересборки образа.
- Главная страница читает `configs/routers.yml` динамически на каждый запрос.
