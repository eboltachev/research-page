global:
  scrape_interval: 15s

scrape_configs:
  - job_name: research-page
    static_configs:
      - targets: ["research-page:8000"]

remote_write:
  - url: ${PROM_REMOTE_WRITE_URL}
    basic_auth:
      username: ${PROM_REMOTE_WRITE_USERNAME}
      password: ${PROM_REMOTE_WRITE_PASSWORD}
